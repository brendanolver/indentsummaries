DATABASE_URL=postgres://user:password@localhost:5432/indentsummaries
# Railway sets DATABASE_URL automatically once a Postgres plugin is attached

AM_TOKEN=replace-with-the-apparelmagic-api-token
# ApparelMagic API token, read-only usage against kohindustries.app.apparelmagic.com

PORT=3000
NODE_ENV=development
